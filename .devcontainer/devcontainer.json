{
  "name": "JupyterLab with LLAMA API",
  "image": "python:3.10",
  "postCreateCommand": "pip install jupyter jupyterlab torch torchvision torchaudio diffusers transformers accelerate scipy ipywidgets tqdm && pip install -r requirements.txt && apt-get install python3-pip && bash install install.sh",
  "customizations": {
      "vscode": {
          "extensions": [
              "ms-python.python",
              "ms-toolsai.jupyter",
              "jupyter.jupyterlab"
          ]
      }
  },
  "forwardPorts": [
      8888,
      9898

  ]
}
